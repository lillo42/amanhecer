name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Amanhecer.slnx

      - name: Build and test (no external dependencies)
        run: |
          set -e
          for project in tests/*/*.csproj; do
            case "$project" in
              # Shared contract-test library (not runnable) and the RabbitMQ/Kafka
              # tests, which need a broker and run in their own jobs.
              *Messaging.Base.Tests*|*RabbitMq.Tests*|*ConfluentKafka.Tests*|*Dekaf.Tests*) continue ;;
            esac
            dotnet test "$project" --configuration Release
          done

  rabbitmq:
    needs: build
    runs-on: ubuntu-latest

    services:
      rabbitmq:
        image: rabbitmq:4-management
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Amanhecer.slnx

      - name: Test RabbitMQ (requires the broker service)
        run: dotnet test tests/Amanhecer.RabbitMq.Tests/Amanhecer.RabbitMq.Tests.csproj --configuration Release

  kafka:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore Amanhecer.slnx

      - name: Start Redpanda
        run: |
          docker run -d --name redpanda -p 9092:9092 \
            docker.redpanda.com/redpandadata/redpanda:v26.2.2 \
            redpanda start --smp=1 --memory=1G --reserve-memory=0M --overprovisioned \
            --node-id=0 --check=false \
            --kafka-addr=PLAINTEXT://0.0.0.0:9092 \
            --advertise-kafka-addr=PLAINTEXT://localhost:9092

      - name: Start Apache Kafka 4 (for the Dekaf tests, which require KIP-848)
        run: |
          docker run -d --name kafka -p 29092:9092 \
            -e KAFKA_NODE_ID=1 \
            -e KAFKA_PROCESS_ROLES=broker,controller \
            -e KAFKA_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093 \
            -e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://localhost:29092 \
            -e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
            -e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT \
            -e KAFKA_CONTROLLER_QUORUM_VOTERS=1@localhost:9093 \
            -e KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 \
            -e KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1 \
            -e KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1 \
            -e KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS=0 \
            apache/kafka:4.0.0

      - name: Wait for the brokers
        run: |
          for i in $(seq 1 30); do
            if docker exec redpanda rpk cluster info >/dev/null 2>&1 \
              && docker exec kafka /opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 >/dev/null 2>&1; then
              break
            fi
            sleep 2
          done
          docker exec redpanda rpk cluster info
          docker exec kafka /opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 >/dev/null
          # Topics are provisioned explicitly by the transports; disable auto-creation so
          # missing-topic validation is meaningful.
          docker exec redpanda rpk cluster config set auto_create_topics_enabled false

      - name: Test Kafka (requires the Redpanda container)
        run: |
          set -e
          dotnet test tests/Amanhecer.ConfluentKafka.Tests/Amanhecer.ConfluentKafka.Tests.csproj --configuration Release
          dotnet test tests/Amanhecer.Dekaf.Tests/Amanhecer.Dekaf.Tests.csproj --configuration Release
